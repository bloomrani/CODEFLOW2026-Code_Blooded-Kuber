import 'dart:typed_data';

class WebDownloader {
  static void downloadPdf(Uint8List bytes, String fileName) {
    // Left completely blank. This stub will only be targeted on mobile platforms, 
    // where the mobile saving logic inside dashboard_screen.dart handles the execution.
  }
}